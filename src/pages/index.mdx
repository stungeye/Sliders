import BaseLayout from "../layouts/BaseLayout.astro";
import ThemeSample from "../components/ThemeSample.astro";
import ReactCounter from "../components/ReactCounter.jsx";

export const layout = BaseLayout;

# Sliders

This scaffold proves that MDX can render Markdown, Astro components, React islands, and syntax-highlighted code blocks.

<ThemeSample />

<ReactCounter client:load />

```html
<main>
  <h1>Accessible teaching notes</h1>
</main>
```
